name = "Wolf Arsenalpoint"
mainprefix = "z"
prefix = "wolf"
author = "Wolf Corps"

[files]
include = [
    "README.md",
    "LICENSE"
]



[version]
git_hash = 0
path = "addons/arsenalpoint/script_version.hpp"

keyname = "{{prefix}}_{{version}}"
signame = "{{prefix}}_{{version}}-{{git \"id 8\"}}"

releasebuild = [
    "@zip wolf_arsenalpoint_{{semver.major}}.{{semver.minor}}.{{semver.patch}}"
]

[header_exts]
version= "{{git \"id 8\"}}"
